#include<bits/stdc++.h>
using namespace std;

int main() {
    deque<int> d = {33, 45, 8, 71};

    cout<< *min_element(d.begin(), d.end()) <<endl; // 8  T.C: O(n)

    auto adress = min_element(d.begin(), d.end());
    cout<<"Index Num: "<< adress - d.begin() <<endl; // index 2

    return 0;
}
